rembg>=2.0.60
onnxruntime>=1.17        # swap for onnxruntime-gpu (NVIDIA) or onnxruntime-directml (any GPU on Windows)
opencv-contrib-python-headless>=4.8   # contrib = extra inpainting tools; uninstall plain opencv-python-headless first
Pillow>=10.1
numpy>=1.24
PySide6>=6.5